<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>info.mengnan</groupId>
        <artifactId>dialoger-ai</artifactId>
        <version>${revision}</version>
    </parent>

    <artifactId>server</artifactId>
    <packaging>jar</packaging>
    <name>server</name>
    <description>Web server for dialoger-ai project</description>

    <properties>
        <!--
            Frontend build is skipped by default for faster local dev iterations.
            Enabled automatically when building with -Prelease-server.
            To force a frontend build: ./mvnw package -Dskip.ui.build=false
        -->
        <skip.ui.build>true</skip.ui.build>
    </properties>

    <dependencies>
        <!-- Internal Module -->
        <dependency>
            <groupId>info.mengnan</groupId>
            <artifactId>common</artifactId>
        </dependency>

        <dependency>
            <groupId>info.mengnan</groupId>
            <artifactId>repository</artifactId>
        </dependency>

        <dependency>
            <groupId>info.mengnan</groupId>
            <artifactId>rag</artifactId>
        </dependency>

        <dependency>
            <groupId>info.mengnan</groupId>
            <artifactId>kb</artifactId>
        </dependency>

        <!-- Spring Boot Starters -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- Spring WebFlux for Reactive Support -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webflux</artifactId>
        </dependency>

        <!-- Validation -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!-- Lombok -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- Sa-Token -->
        <dependency>
            <groupId>cn.dev33</groupId>
            <artifactId>sa-token-spring-boot3-starter</artifactId>
        </dependency>

        <dependency>
            <groupId>cn.dev33</groupId>
            <artifactId>sa-token-jwt</artifactId>
        </dependency>


        <!-- Test -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <resources>
            <resource>
                <directory>src/main/resources</directory>
            </resource>
        </resources>

        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <version>${spring-boot.version}</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>repackage</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <mainClass>info.mengnan.dialogerai.server.DialogerAiApplication</mainClass>
                </configuration>
            </plugin>

            <!--
                Frontend build pipeline.
                Phase: generate-resources (runs before process-resources copies ui/dist → static/).
                Node runtime is installed once under the project root and reused across builds.
            -->
            <plugin>
                <groupId>com.github.eirslett</groupId>
                <artifactId>frontend-maven-plugin</artifactId>
                <version>1.15.0</version>
                <configuration>
                    <workingDirectory>../ui</workingDirectory>
                    <installDirectory>${user.home}/.frontend-maven-plugin</installDirectory>
                    <nodeVersion>v20.19.0</nodeVersion>
                    <nodeDownloadRoot>https://registry.npmmirror.com/-/binary/node/</nodeDownloadRoot>
                    <skip>${skip.ui.build}</skip>
                </configuration>
                <executions>
                    <execution>
                        <id>install-node-and-npm</id>
                        <goals>
                            <goal>install-node-and-npm</goal>
                        </goals>
                        <phase>generate-resources</phase>
                    </execution>
                    <execution>
                        <id>npm-install</id>
                        <goals>
                            <goal>npm</goal>
                        </goals>
                        <phase>generate-resources</phase>
                        <configuration>
                            <arguments>install</arguments>
                        </configuration>
                    </execution>
                    <execution>
                        <id>npm-build</id>
                        <goals>
                            <goal>npm</goal>
                        </goals>
                        <phase>generate-resources</phase>
                        <configuration>
                            <!-- build-only = vite build, skips type-check for faster CI builds -->
                            <arguments>run build-only</arguments>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <!--
            release-server: Enable frontend build when assembling a release package.
            Activated alongside the same-named profile in the distribution module via:
              ./mvnw clean package -Prelease-server
        -->
        <profile>
            <id>release-server</id>
            <properties>
                <skip.ui.build>false</skip.ui.build>
            </properties>
            <build>
                <resources>
                    <resource>
                        <directory>src/main/resources</directory>
                    </resource>
                    <!--
                        Frontend build output (ui/dist/) is bundled into
                        BOOT-INF/classes/static/ inside the executable fat-jar.
                        Spring Boot will serve these files as static resources.
                    -->
                    <resource>
                        <directory>../ui/dist</directory>
                        <targetPath>static</targetPath>
                    </resource>
                </resources>
            </build>
        </profile>
    </profiles>

</project>